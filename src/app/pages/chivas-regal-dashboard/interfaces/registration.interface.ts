export interface IRegistration {
  _id?: string;
  name: string;
  email: string;
  address: string;
  phone: string;
  dateOfBirth: string;
  local: string;
  favoriteDrink: string;
  createdAt: string;
}
